{
  "name": "@citecast/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "VITE_ENTRY=content vite build && VITE_ENTRY=popup vite build",
    "dev": "VITE_ENTRY=content vite build --watch"
  },
  "dependencies": {
    "@citecast/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
